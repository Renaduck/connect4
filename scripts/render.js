const Models = require('./models')

function renderPiece(piece) {
  const pieceNode = {};
  return pieceNode;  // should be a HTML dom node
}

function renderBoard(board) {
  // use `renderPiece to make board HTML dom node
  
  const boardNode = {};
  return boardNode; // should be a HTML dom node
}

function renderGame(game) {
  // use `renderBoard to make game HTML dom node
  var gameDiv = document.getElementById("gameDiv")
  const gameNode = {};
  return gameNode; // should be a HTML dom node
}

export default {
  renderGame
}
